- **EC2**
	- AWS services either regional or global and EC2 is only regional.
- On-Premises: handled by ours.
- In AWS LB never go  down (ELB- Elastic Load Balancer)
- ELB is a service not a server.
- you cant login to ELB only you can access by URL
- ELB doesn't have availability zone it created at.
- Which service is used for quick and easy to deploy ?
	- Ans : EBS(Elastic bean Stalk)
- In Elastic Bean Stalk we have the control on the server.
	- In general Platform As service we don't have any control on server.
- If you don't want to do every thing : EBS or ELB
- If  you want to do every thing yourself : EC2
## Light Sail (Not Support Auto Scaling)
- **Very easy to Handle**
- If you  want to setup and create a virtual private server  which already has everything installed(WordPress, Joomla, Drupal, Cpanel, Gitlab, Redmine, etc).
## Lambda (Server Less)
 - When ec2 instance lunch any event the lambda function will triggered automatically.
 - Lambda is invoked based on the event trigger.
 - Lambda is used for automate.