- Once we create a user one user signing link will be generated using that link he can login.
- We can disable the IAM user console access also from security credentials.
- Manage console access > enable / disable
**MFA configuration**
- Assign MFA device > select MFA device > choose option > Authenticator App > next 
- Show QR code > scan QR code by mobile > provide code > add MFA (I can remove, resync)
- 1 is for root account and 1 for IAM account
**Access Keys**
- Create Access keys > choose the option (CLI)> check understand > next > (tags) optional > create
- Note : One time will generate you have to save some where 
- Max we can create 2 only
	**Disable the Access keys **
	- Actions >Deactivate >
	- You can disable the programmatical access by deactivating the keys.
- 