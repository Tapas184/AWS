- I can attached permission to Group and individual also.
- We can't give MFA for a group.
- IAM Group = collection of IAM group.
- Group under group is not possible / Nested groups are not possible.
- It is possible to attach multiple polices to the IAM user and groups also, MAX 10 you can attach and detach the polices to the IAM users and Groups.
- If you attach any IAM user to the IAM group. IAM user individual policies remains same and the new permission will  be inherited to the IAM user.
- You cannot assign / create keys to the group
- KEYS are only for IAM users not for IAM groups
- An IAM user can be attached to multiple groups at the same time.
- Brand new IAM user will not have all permission / policies attached by default.
### Policies 
- Policies -> Policy Document -> Policy document contains permission.
- Policies contains permission
- Policies are written in JSON format.
- Using policy generator we can generate policy
**Managed Policy**
- Predefined policies created by AWS and managed by AWS
**Inline Policy** (Customer managed policy)
- Created and managed by customer  
#### ARN (Amazon Resource Name)
- Based on this we can remember the IAM user role
## IAM Roles (Important)
- Temporary access without credentials
- From EC2 instance if you want to access AWS you have to install AWS cli
- using KEYS we have to access the AWS.
- It will  be stored inside EC2 instance
- Not recommended to configure keys inside ec2 instance.
- IAM user is used wo login  / access AWS console not the servers.
- If we use the role no need to configure KEYS on the EC2 instance.
- Based on the  permission that you have attached to the ROLE, those permission are available from the EC2 instance.
- EC2 Instance can have only 1 Role attached.
- Role can be attached to multiple EC2 instance
### Identity Provider / Federation / IAM Identity center
